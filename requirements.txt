# BrainPass — dependencies
#
# Core librarian uses Python standard library + sqlite3 (stdlib).
# BM25 search works with zero pip dependencies.
#
# Optional: semantic search (ChromaDB) — enables hybrid BM25+vector search
# Install with: pip install chromadb importlib-metadata
#
chromadb>=1.0.0
importlib-metadata        # Python 3.14 compat for opentelemetry
#
# Other optional niceties:
#   requests        — if you prefer it over urllib
#   rich            — for pretty CLI output
#   watchdog        — for live vault reindexing
